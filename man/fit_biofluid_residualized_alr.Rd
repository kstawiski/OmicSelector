% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper3-biofluid-aware-scoring.R
\name{fit_biofluid_residualized_alr}
\alias{fit_biofluid_residualized_alr}
\title{Fit biofluid-residualized ALR}
\usage{
fit_biofluid_residualized_alr(
  train_expr,
  train_meta,
  panel,
  biofluid_col = "biofluid",
  outcome_col = "disease",
  cohort_col = "cohort",
  pivot_features = NULL,
  pseudocount = NULL,
  min_pivot_present = 3L
)
}
\arguments{
\item{train_expr}{Numeric matrix, training samples x features.}

\item{train_meta}{Data frame aligned to `train_expr`.}

\item{panel}{Character vector of panel features.}

\item{biofluid_col}{Column identifying biofluid class.}

\item{outcome_col}{Binary disease label column in `train_meta`.}

\item{cohort_col}{Cohort column in `train_meta`.}

\item{pivot_features}{Optional ALR pivot features.}

\item{pseudocount}{Optional additive pseudocount before log transform.}

\item{min_pivot_present}{Minimum pivot features required for prediction.}
}
\description{
Fits one per-feature training model,
`log(feature_count) ~ disease + biofluid + (1 | cohort)`, subtracts the
estimated biofluid fixed effect at prediction time, and scores the panel as
an ALR against a frozen pivot pool. If `lme4` is unavailable or a feature
model fails, an `lm()` fallback is used and recorded.
}
