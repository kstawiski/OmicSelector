% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias-audit.R
\name{os_bias_audit}
\alias{os_bias_audit}
\title{One-Shot Bias Audit Report}
\usage{
os_bias_audit(
  X,
  y,
  cohort,
  predictions = NULL,
  covariates = NULL,
  specimen_id = NULL,
  sample_id = NULL,
  feature_names = NULL,
  seed = 42L
)
}
\arguments{
\item{X}{Feature matrix (n x p).}

\item{y}{Binary outcome.}

\item{cohort}{Cohort/dataset identity per sample.}

\item{predictions}{Optional numeric vector of model predictions (n).}

\item{covariates}{Optional named list of numeric or factor covariates
(e.g., list(age = age_years, sex = sex_factor)).}

\item{specimen_id}{Optional specimen-level IDs for duplicate detection.}

\item{sample_id}{Optional sample-level IDs used when reporting duplicate
pairs. If omitted, row names of \code{X} are used when available.}

\item{feature_names}{Optional feature labels.}

\item{seed}{Random seed.}
}
\value{
An \code{os_bias_audit} object (list) with fields:
  \code{bias_floor}, \code{covariate_aucs}, \code{feature_signal},
  \code{duplicates}, \code{provenance_summary},
  \code{classifier_vs_floor} (if predictions provided).
}
\description{
Orchestrator that runs all bias-audit diagnostics on a model + data bundle
and returns a structured report. Intended to be emitted as a first-class
output of every OmicSelector biomarker analysis.
}
