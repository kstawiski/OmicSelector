% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TabularDL.R
\name{make_catboost_learner}
\alias{make_catboost_learner}
\title{Create a CatBoost Learner}
\usage{
make_catboost_learner(
  iterations = 500L,
  learning_rate = 0.03,
  depth = 6L,
  l2_leaf_reg = 5,
  border_count = 64L,
  patience = 40L,
  validation_fraction = 0.15,
  class_weights = "balanced",
  device = c("auto", "cpu", "cuda"),
  seed = 20260331L
)
}
\arguments{
\item{iterations}{Maximum boosting iterations. Default: `500`.}

\item{learning_rate}{Learning rate. Default: `0.03`.}

\item{depth}{Tree depth. Default: `6`.}

\item{l2_leaf_reg}{L2 regularization on leaf values. Default: `5`.}

\item{border_count}{Number of numerical split candidates. Default: `64`.}

\item{patience}{Overfitting detector patience for the internal validation
split. Default: `40`.}

\item{validation_fraction}{Fraction of the training fold reserved for
early-stopping. Default: `0.15`.}

\item{class_weights}{Class weighting scheme. Use `"balanced"` (default),
`"none"`, or a numeric vector with one weight per class.}

\item{device}{`"auto"` (default), `"cpu"`, or `"cuda"`.}

\item{seed}{Random seed used inside the backend. Default: `20260331`.}
}
\value{
An `mlr3::LearnerClassif` with `predict_type = "prob"`.
}
\description{
Create an `mlr3` classification learner backed by the `catboost` R package.
}
\references{
Prokhorenkova, L., et al. (2018). CatBoost: unbiased boosting with
categorical features.
}
