% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coda-feature-selection.R
\name{codaFS_codacore_wrapper}
\alias{codaFS_codacore_wrapper}
\title{CoDaCoRe-Derived Feature Selection}
\usage{
codaFS_codacore_wrapper(
  X,
  y,
  n_features = NULL,
  positive = NULL,
  input_scale = c("log", "raw"),
  backend = c("auto", "official", "fallback"),
  max_balances = NULL,
  top_k = 3L,
  class_weight = FALSE,
  verbose = FALSE,
  seed = 42L
)
}
\arguments{
\item{X}{Numeric matrix with samples in rows and features in columns.}

\item{y}{Binary outcome vector.}

\item{n_features}{Optional target panel size.}

\item{positive}{Optional positive-class label for non-numeric outcomes.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{backend}{One of `"auto"`, `"official"`, or `"fallback"`.}

\item{max_balances}{Maximum number of balances to extract.}

\item{top_k}{Initial sparse balance size for the fallback backend.}

\item{class_weight}{Logical; passed to the fallback backend.}

\item{verbose}{Logical; print backend messages.}

\item{seed}{Integer random seed.}
}
\value{
A list with per-feature `scores`, `selected_features`, extracted
  balances, and backend details.
}
\description{
Selects features appearing in sparse CoDaCoRe balances. The official
`codacore` backend is used when available and functional; otherwise the
existing OmicSelector sparse-balance fallback is reused. Feature scores are
aggregated across balances using absolute slopes when available.
}
