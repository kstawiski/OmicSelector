% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singlesample-provenance-aware-scoring.R
\name{score_cohort_z_then_pool_score}
\alias{score_cohort_z_then_pool_score}
\title{Score a panel after cohort-wise robust z-standardization}
\usage{
score_cohort_z_then_pool_score(
  expr,
  meta,
  panel,
  cohort_col = "cohort",
  block_col = "provenance_block",
  fit = NULL,
  allow_new_group_moments = TRUE,
  pseudocount = NULL
)
}
\arguments{
\item{expr}{Numeric matrix, samples x features.}

\item{meta}{Data frame aligned to `expr`.}

\item{panel}{Character vector of panel features.}

\item{cohort_col}{Column in `meta` identifying cohorts.}

\item{block_col}{Column in `meta` identifying provenance blocks; accepted for
a common scorer signature but not used by this method.}

\item{fit}{Optional object from `fit_cohort_z_then_pool_score()`.}

\item{allow_new_group_moments}{Logical; if TRUE, new cohorts are centered on
their own unlabeled samples.}

\item{pseudocount}{Optional additive pseudocount before log transform.}
}
\value{
Numeric score vector, one value per sample.
}
\description{
Within each cohort, all panel features are log-transformed, centered by the
cohort median, scaled by cohort MAD, and summed. When `fit` lacks a new test
cohort, the default is to compute unsupervised moments from that test cohort;
no disease labels are used for this fallback.
}
