% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hemolysis-correction.R
\name{train_weighted_clr_mlp}
\alias{train_weighted_clr_mlp}
\title{Train a Weighted CLR + MLP Classifier}
\usage{
train_weighted_clr_mlp(
  X_train,
  y_train,
  weights = NULL,
  sensitivity = .paper1_hemolysis_coefficients(),
  shrink_features = TRUE,
  epochs = 30L,
  lr = 0.001,
  batch_size = 256L,
  class_weight = TRUE,
  device = NULL,
  verbose = TRUE,
  seed = 42L,
  positive = NULL,
  input_scale = c("log", "raw"),
  backend = c("auto", "torch", "nnet", "glm"),
  hidden_units = c(152L, 136L),
  dropout = 0.3
)
}
\arguments{
\item{X_train}{Numeric training matrix with samples in rows and features in
columns.}

\item{y_train}{Binary outcome vector.}

\item{weights}{Optional positive numeric vector of feature weights.}

\item{sensitivity}{Optional named positive numeric vector of hemolysis
coefficients used when `weights = NULL`.}

\item{shrink_features}{Logical; whether to shrink centered coordinates by
feature weight after weighted centering.}

\item{epochs}{Number of training epochs. Defaults to `30`.}

\item{lr}{Learning rate. Defaults to `0.001`.}

\item{batch_size}{Mini-batch size. Defaults to `256`.}

\item{class_weight}{Logical; if `TRUE`, use inverse-frequency weighting.}

\item{device}{One of `"cpu"`, `"cuda"`, or `NULL`/`"auto"`.}

\item{verbose}{Logical; print training progress. Defaults to `TRUE`.}

\item{seed}{Integer random seed. Defaults to `42`.}

\item{positive}{Optional positive-class label for non-numeric outcomes.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{backend}{One of `"auto"`, `"torch"`, `"nnet"`, or `"glm"`.}

\item{hidden_units}{Integer vector with the two hidden-layer sizes used by
the torch backend. Defaults to `c(152, 136)`.}

\item{dropout}{Dropout rate used by the torch backend. Defaults to `0.3`.}
}
\value{
An object of class `"omicselector_weighted_clr_mlp"`.
}
\description{
Fits a hemolysis-aware weighted-CLR analogue of [train_clr_mlp()] using the
same backends and normalization scheme as the dense CoDA baseline.
}
