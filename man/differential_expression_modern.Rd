% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/differential-expression-modern.R,
%   R/modern-differential-expression.R
\name{differential_expression_modern}
\alias{differential_expression_modern}
\title{Modern Differential Expression Analysis}
\usage{
differential_expression_modern(
  expression_data,
  class_labels,
  data_type = "auto",
  case_label = "Case",
  control_label = "Control",
  p_adjust_methods = c("BH", "bonferroni", "holm"),
  alpha = 0.05,
  min_samples_per_group = 3,
  effect_size_methods = c("cohens_d", "hedges_g"),
  parallel = TRUE,
  verbose = TRUE
)

differential_expression_modern(
  expression_data,
  class_labels,
  data_type = "auto",
  case_label = "Case",
  control_label = "Control",
  p_adjust_methods = c("BH", "bonferroni", "holm"),
  alpha = 0.05,
  min_samples_per_group = 3,
  effect_size_methods = c("cohens_d", "hedges_g"),
  parallel = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{expression_data}{Matrix or data.frame with features in columns, samples in rows}

\item{class_labels}{Vector of class labels ("Case"/"Control" or custom labels)}

\item{data_type}{Character: "log_tpm", "delta_ct", "counts", or "auto"}

\item{case_label}{Character: label for cases (default: "Case")}

\item{control_label}{Character: label for controls (default: "Control")}

\item{p_adjust_methods}{Character vector: correction methods to apply}

\item{alpha}{Numeric: significance threshold (default: 0.05)}

\item{min_samples_per_group}{Integer: minimum samples required per group}

\item{effect_size_methods}{Character vector: effect size measures to calculate}

\item{parallel}{Logical: use parallel processing for large datasets}

\item{verbose}{Logical: enable progress messages}

\item{features}{Matrix or data frame with features as columns and samples as rows}

\item{classes}{Factor or character vector with sample classes ("Case", "Control")}

\item{mode}{Character string: "logtpm", "deltact", or "auto"}

\item{correction_method}{Character string: "BH" (default), "bonferroni", "holm", etc.}

\item{log_fold_change}{Logical: whether to calculate log fold change (default TRUE)}
}
\value{
Data frame with differential expression results

Data.frame with comprehensive differential expression results including
  p-values, adjusted p-values, fold changes, effect sizes, and confidence intervals
}
\description{
Modernized differential expression analysis using t-test with multiple testing correction.
This function replaces the excessive suppressMessages() usage and provides better
error handling and logging.

Modernized version of differential expression analysis with improved statistical
methods, comprehensive error handling, and enhanced result reporting. Supports
both traditional t-test and modern statistical approaches.

Key improvements:
- Removed 24 redundant library calls, using only essential packages
- Added comprehensive input validation and error handling
- Enhanced statistical methods with effect size calculations
- Improved multiple testing correction options
- Memory-efficient processing for large datasets
- Modern tidyverse-style data handling
- Comprehensive result reporting with confidence intervals
- Support for different data types (log-TPM, delta-Ct, etc.)
}
\examples{
\dontrun{
# Create example data
set.seed(123)
features <- matrix(rnorm(1000), nrow = 50, ncol = 20)
colnames(features) <- paste0("feature_", 1:20)
classes <- factor(rep(c("Case", "Control"), each = 25))

# Run differential expression
results <- differential_expression_modern(features, classes)
}

\dontrun{
# Basic differential expression
results <- differential_expression_modern(
  expression_data = mirna_matrix,
  class_labels = sample_classes
)

# Advanced analysis with custom parameters
results <- differential_expression_modern(
  expression_data = expression_matrix,
  class_labels = groups,
  data_type = "log_tpm",
  p_adjust_methods = c("BH", "bonferroni", "holm"),
  effect_size_methods = c("cohens_d", "hedges_g", "cliff_delta"),
  alpha = 0.01
)

# Filter significant results
significant <- results[results$p_value_BH < 0.05, ]
}

}
