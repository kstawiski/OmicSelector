% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singlesample-selector.R
\name{fit_singlesample_selector}
\alias{fit_singlesample_selector}
\alias{print.singlesample_selector_set}
\alias{print.singlesample_selector_ineligible}
\title{Fit a leakage-resistant single-sample method selector}
\usage{
fit_singlesample_selector(
  X_train,
  y_train,
  methods = NULL,
  route = c("best_auc", "lower_auc_ci", "simplex_stack", "all"),
  group_id = NULL,
  meta_train = NULL,
  annotation = NULL,
  inner_folds = 5L,
  seed = 1L,
  bootstrap_reps = 1000L,
  verify_base = TRUE,
  stack_stabilizer = 1e-06
)

\method{print}{singlesample_selector_set}(x, ...)

\method{print}{singlesample_selector_ineligible}(x, ...)
}
\arguments{
\item{X_train}{Numeric matrix or data frame (specimens x features).}

\item{y_train}{Binary training outcome.}

\item{methods}{Character method identifiers. `NULL` uses
[singlesample_selector_candidates()].}

\item{route}{One of `"best_auc"`, `"lower_auc_ci"`, `"simplex_stack"`, or
`"all"`. The `"all"` route reuses one cross-fitting pass and returns a
`singlesample_selector_set` containing all three prespecified routes.}

\item{group_id}{Biological/provenance group per row. Rows in one group are
never split across inner folds. Defaults to one group per row.}

\item{meta_train}{Optional training metadata, one row per specimen.}

\item{annotation}{Optional feature annotation passed to compatible base
methods.}

\item{inner_folds}{Requested number of grouped inner folds. It is reduced to
the smaller class-specific group count and must remain at least two.}

\item{seed}{Integer seed for fold construction and deterministic derived
per-method seeds.}

\item{bootstrap_reps}{Number of stratified biological-group bootstrap draws
for `lower_auc_ci`.}

\item{verify_base}{Whether [deploy_singlesample()] should run its row-
equivariance gate for every base fit.}

\item{stack_stabilizer}{Fixed non-negative L2 numerical stabilizer for the
simplex stack. It is not tuned from outcomes.}

\item{x}{A `singlesample_selector_set` object.}

\item{...}{Unused.}
}
\value{
A `singlesample_selector`, `singlesample_selector_set`, or a
  `singlesample_selector_ineligible` object with an explicit audit when the
  requested route cannot be fitted.
}
\description{
Fits an experimental meta-scorer that selects or combines registered
single-sample methods using grouped cross-fitted predictions from the
supplied training data only. The fitted object scores each new specimen from
frozen base models; it never uses a co-resident test batch or test outcomes.

Three prespecified routes are available:
\describe{
  \item{`best_auc`}{Select the method with the largest mean grouped inner-fold
    AUC.}
  \item{`lower_auc_ci`}{Select the method with the largest lower 95 percent
    stratified group-bootstrap limit for pooled cross-fitted AUC.}
  \item{`simplex_stack`}{Fit a non-negative logistic stack whose weights sum
    to one, using cross-fitted group-level scores and a fixed numerical
    stabilizer. No stack hyperparameter is tuned.}
}

This API establishes a leakage-resistant fitting contract; it is not a claim
that selection or stacking improves discrimination. Such a claim requires a
separate outer validation.
}
\seealso{
[score_singlesample_selector()], [deploy_singlesample()]
}
