% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singlesample-method-roster.R
\name{singlesample_register_score_adapter}
\alias{singlesample_register_score_adapter}
\title{Register a non-canonical score adapter for a roster method}
\usage{
singlesample_register_score_adapter(method_id, fn)
}
\arguments{
\item{method_id}{Roster method identifier.}

\item{fn}{A function `function(model, X, meta)` returning one numeric score
per row of `X`.}
}
\value{
Invisibly `TRUE`.
}
\description{
New roster methods follow the canonical convention
`score_*(model, X, meta = NULL)` and need no adapter. Legacy ("present")
methods whose score function uses a different argument order register a shim
mapping them onto the canonical `function(model, X, meta)` signature.
}
\seealso{
[singlesample_score_call()]
}
