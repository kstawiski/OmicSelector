% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias-audit.R
\name{os_clustered_bootstrap_auc}
\alias{os_clustered_bootstrap_auc}
\title{Clustered Bootstrap CI for AUC}
\usage{
os_clustered_bootstrap_auc(
  y,
  predictions,
  cluster_id,
  n_boot = 2000L,
  stratify_by = NULL,
  ci_type = c("two-sided", "one-sided-lower"),
  alpha = 0.05,
  seed = 42L
)
}
\arguments{
\item{y}{Binary outcome vector.}

\item{predictions}{Numeric predicted probabilities / scores.}

\item{cluster_id}{Vector of cluster identifiers (length n).}

\item{n_boot}{Number of bootstrap replicates. Default 2000.}

\item{stratify_by}{Optional factor for stratified resampling
(e.g., fold x class). Strata are preserved at the cluster level.}

\item{ci_type}{One of \code{"two-sided"} or \code{"one-sided-lower"}.
Default \code{"two-sided"}.}

\item{alpha}{Significance level (default 0.05).}

\item{seed}{Random seed.}
}
\value{
A list with the point AUC and bootstrap CI bounds.
}
\description{
Bootstrap the AUC of a set of predictions respecting within-cluster
dependence (e.g., duplicated specimens with the same internal ID across
accessions; repeated measures from the same patient). Rows with the same
cluster ID are resampled as a unit, which preserves the within-cluster
correlation structure and prevents anti-conservative CIs.
}
