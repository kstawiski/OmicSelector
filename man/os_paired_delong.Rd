% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-comparison.R
\name{os_paired_delong}
\alias{os_paired_delong}
\title{Paired DeLong Test for AUC Comparison}
\usage{
os_paired_delong(
  y_true,
  scores_a,
  scores_b,
  alternative = c("two.sided", "greater", "less")
)
}
\arguments{
\item{y_true}{Integer or logical vector of true labels (0/1).}

\item{scores_a}{Numeric vector of scores from pipeline A; same length as
\code{y_true}.}

\item{scores_b}{Numeric vector of scores from pipeline B; same length as
\code{y_true}.}

\item{alternative}{One of \code{"two.sided"} (default), \code{"greater"}
(A's AUC > B's), or \code{"less"}.}
}
\value{
A named list with elements \code{auc_a}, \code{auc_b}, \code{delta}
  (= auc_a - auc_b), \code{z}, \code{p_value}, \code{ci_delta} (95% CI of
  the AUC difference; \code{NA} if pROC does not return one), \code{n_pos},
  \code{n_neg}, \code{alternative}, and \code{method} (= "DeLong").
}
\description{
Compare two prediction-score vectors on the same labels using the paired
DeLong test (Robin et al. 2011 implementation in \code{\link[pROC]{roc.test}}).
}
\examples{
set.seed(7)
y <- rep(c(0, 1), each = 50)
a <- y + rnorm(100, sd = 0.5)            # informative
b <- rnorm(100)                          # random
os_paired_delong(y, a, b, alternative = "greater")

}
