% Generated by hand
\name{create_autoencoder_pipeop}
\alias{create_autoencoder_pipeop}
\title{Create Autoencoder PipeOp}
\usage{
create_autoencoder_pipeop(latent_dim = 32L, hidden_layers = c(128L, 64L),
  activation = "relu", dropout = 0.1, epochs = 100L, batch_size = 32L,
  lr = 0.001, weight_decay = 0, loss = "mse", validation_split = 0.1,
  early_stopping = TRUE, patience = 10L, min_delta = 0, device = "cpu",
  seed = NULL, pretrained = NULL, freeze_encoder = FALSE, concat = FALSE,
  prefix = "ae_")
}
\arguments{
\item{latent_dim}{Integer, size of latent representation.}
\item{hidden_layers}{Integer vector of hidden layer sizes.}
\item{activation}{Activation function.}
\item{dropout}{Dropout rate between 0 and 1.}
\item{epochs}{Number of training epochs.}
\item{batch_size}{Batch size.}
\item{lr}{Learning rate.}
\item{weight_decay}{L2 weight decay.}
\item{loss}{Loss function: "mse" or "mae".}
\item{validation_split}{Fraction of data for validation.}
\item{early_stopping}{Logical, enable early stopping.}
\item{patience}{Early stopping patience.}
\item{min_delta}{Minimum improvement to reset patience.}
\item{device}{"cpu" or "cuda".}
\item{seed}{Optional random seed.}
\item{pretrained}{Optional path to a saved autoencoder state.}
\item{freeze_encoder}{Logical, freeze encoder weights during training.}
\item{concat}{Logical, if TRUE returns original + latent features.}
\item{prefix}{Prefix for latent feature names.}
}
\value{
A PipeOp object.
}
\description{
Creates an mlr3pipelines PipeOp that trains a torch autoencoder on the
training data and replaces features with latent representations.
}
