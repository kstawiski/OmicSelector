% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hemolysis-correction.R
\name{weighted_clr_transform}
\alias{weighted_clr_transform}
\title{Weighted Centered Log-Ratio Transformation}
\usage{
weighted_clr_transform(
  x,
  weights = NULL,
  sensitivity = .paper1_hemolysis_coefficients(),
  input_scale = c("log", "raw"),
  pseudocount = 1e-08,
  shrink_features = TRUE,
  feature_names = NULL
)
}
\arguments{
\item{x}{Numeric vector or matrix with samples in rows and features in
columns.}

\item{weights}{Optional positive numeric vector of feature weights. If named,
names are matched to `feature_names`.}

\item{sensitivity}{Optional named positive numeric vector of hemolysis
coefficients. Used only when `weights = NULL`.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{pseudocount}{Small positive constant added before logging raw inputs.}

\item{shrink_features}{Logical; if `TRUE`, multiply the centered coordinates
by their feature weights after centering.}

\item{feature_names}{Optional feature names.}
}
\value{
Numeric vector or matrix of weighted CLR features.
}
\description{
Computes a weighted within-sample CLR transform. The weighted center uses
down-weighted hemolysis-sensitive features, and the transformed coordinates
can optionally be shrunk by the same weights before classification.
}
