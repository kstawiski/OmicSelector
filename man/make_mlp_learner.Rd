% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DeepLearners.R
\name{make_mlp_learner}
\alias{make_mlp_learner}
\title{Create Omics-Optimized MLP Learner}
\usage{
make_mlp_learner(
  n_hidden = 128L,
  n_layers = 2L,
  dropout = 0.5,
  batch_size = 32L,
  epochs = 100L,
  learning_rate = 0.001,
  weight_decay = 0.01,
  early_stopping_patience = 10L
)
}
\arguments{
\item{n_hidden}{Number of hidden units per layer (default: 128)}

\item{n_layers}{Number of hidden layers (default: 2)}

\item{dropout}{Dropout rate (default: 0.5)}

\item{batch_size}{Training batch size (default: 32)}

\item{epochs}{Number of training epochs (default: 100)}

\item{learning_rate}{Learning rate (default: 0.001)}

\item{weight_decay}{L2 regularization (default: 0.01)}

\item{early_stopping_patience}{Patience for early stopping (default: 10)}
}
\value{
An mlr3 Learner object
}
\description{
Creates a Multi-Layer Perceptron with regularization optimized for
high-dimensional omics data.
}
\details{
The MLP is configured with:
- High dropout (0.5) to prevent overfitting on high-dimensional data
- Strong L2 regularization
- Batch normalization between layers
- Adam optimizer with early stopping
}
\examples{
\dontrun{
learner <- make_mlp_learner(n_hidden = 64, dropout = 0.5)
learner$train(task)
}

}
