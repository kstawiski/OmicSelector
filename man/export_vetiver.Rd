% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-export.R
\name{export_vetiver}
\alias{export_vetiver}
\title{Export Model as Vetiver for Deployment}
\usage{
export_vetiver(
  learner,
  model_name,
  task,
  description = NULL,
  metadata = NULL,
  board = NULL
)
}
\arguments{
\item{learner}{A trained mlr3 Learner or GraphLearner}

\item{model_name}{Name for the vetiver model (required)}

\item{task}{The mlr3 Task used for training (for metadata extraction)}

\item{description}{Optional description string}

\item{metadata}{Optional list of additional metadata}

\item{board}{Optional pins board for immediate deployment. If NULL, returns
the vetiver model without pinning.}
}
\value{
A vetiver_model object (or invisibly if board is provided)
}
\description{
Wraps an mlr3 Learner or GraphLearner in a vetiver model object for
versioning and deployment. Vetiver provides model versioning, API generation,
and monitoring capabilities.
}
\examples{
\dontrun{
library(vetiver)
library(pins)

# Train model
task <- tsk("iris")
learner <- lrn("classif.ranger", predict_type = "prob")
learner$train(task)

# Export as vetiver
v <- export_vetiver(
  learner = learner,
  model_name = "iris_classifier",
  task = task,
  description = "Random Forest classifier for iris dataset"
)

# Pin to a board
board <- board_folder("models", versioned = TRUE)
vetiver_pin_write(board, v)

# Or export and pin in one step
export_vetiver(learner, "iris_classifier", task, board = board)
}

}
