% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DeepLearners.R
\name{make_gnn_learner}
\alias{make_gnn_learner}
\title{Create GNN Learner for Pathway-Aware Classification}
\usage{
make_gnn_learner(
  adjacency_matrix = NULL,
  n_hidden = 64L,
  n_layers = 2L,
  dropout = 0.3,
  aggregation = "mean",
  epochs = 100L
)
}
\arguments{
\item{adjacency_matrix}{Adjacency matrix defining feature relationships
(e.g., from pathway databases, correlation, or PPI networks)}

\item{n_hidden}{Number of hidden units (default: 64)}

\item{n_layers}{Number of GNN layers (default: 2)}

\item{dropout}{Dropout rate (default: 0.3)}

\item{aggregation}{Message aggregation: "mean", "sum", "max"}

\item{epochs}{Number of epochs (default: 100)}
}
\value{
An mlr3 Learner object or a custom GNN wrapper
}
\description{
Creates a Graph Neural Network that incorporates biological pathway
information for omics classification.
}
\details{
The GNN uses:
- Graph Convolutional layers to propagate information
- Feature relationships from biological networks
- Node-level predictions aggregated to graph-level
}
\examples{
\dontrun{
# Create adjacency from correlation
cor_mat <- cor(data)
adj <- (abs(cor_mat) > 0.7) * 1
diag(adj) <- 0

learner <- make_gnn_learner(adj)
learner$train(task)
}

}
\references{
Kipf & Welling (2017). Semi-Supervised Classification with Graph
Convolutional Networks.
}
