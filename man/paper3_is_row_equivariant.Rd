% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper3-method-roster.R
\name{paper3_is_row_equivariant}
\alias{paper3_is_row_equivariant}
\title{Test whether a scorer is row-equivariant (non-throwing)}
\usage{
paper3_is_row_equivariant(
  score_fun,
  model,
  X,
  meta = NULL,
  n_perm = 4L,
  tol = 1e-08,
  n_singletons = 4L,
  model_digest = NULL,
  seed = 1L
)
}
\arguments{
\item{score_fun}{A function `function(model, X, meta)` returning one numeric
score per row of `X`.}

\item{model}{A fitted model object.}

\item{X}{Numeric matrix (samples x features), at least 4 rows.}

\item{meta}{Optional per-sample metadata (data frame with `nrow(X)` rows).}

\item{n_perm}{Number of random row permutations to test.}

\item{tol}{Absolute tolerance for score agreement (default 1e-8).}

\item{n_singletons}{Number of distinct rows to test as singletons (always
includes the first and last row). Pass `nrow(X)` for an exhaustive sweep.}

\item{model_digest}{Optional `function(model)` returning a comparable snapshot
of the model's mutable state. Defaults to a serialization snapshot; supply a
custom closure for torch / external-pointer models that do not serialize
deterministically.}

\item{seed}{Random seed for reproducible permutations/subsets.}
}
\value{
`TRUE` if row-equivariant, `FALSE` otherwise.
}
\description{
Convenience wrapper around [paper3_assert_row_equivariant()] returning a
logical instead of erroring. Used both for affirmative checks and for the
negative test that a deliberately batch-coupled scorer (e.g. the batchwise
Sinkhorn plan) is *not* row-equivariant.
}
\seealso{
[paper3_assert_row_equivariant()]
}
