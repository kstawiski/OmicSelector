% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OmicSelector_OmicSelector_improved.R
\name{OmicSelector_OmicSelector}
\alias{OmicSelector_OmicSelector}
\title{Improved OmicSelector_OmicSelector Function}
\usage{
OmicSelector_OmicSelector(
  wd = getwd(),
  m = c(1:20),
  max_iterations = 10,
  code_path = system.file("extdata", "", package = "OmicSelector"),
  register_parallel = TRUE,
  clx = NULL,
  stamp = as.numeric(Sys.time()),
  prefer_no_features = 20,
  conda_path = "/home/konrad/anaconda3/bin/conda",
  debug = FALSE,
  timeout_sec = 1800,
  type = "auto",
  class_column = "Class",
  case_label = "Case",
  control_label = "Control",
  feature_prefix = "hsa_",
  p_threshold = 0.05,
  fc_threshold = 1,
  use_smote = TRUE,
  save_intermediate = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{wd}{Working directory with data files (mixed_train.csv, mixed_test.csv, mixed_validation.csv)}

\item{m}{Methods of feature selection to be performed (integer vector 1-70)}

\item{max_iterations}{Maximum number of iterations in selected methods}

\item{code_path}{Path to external scripts (legacy parameter, maintained for compatibility)}

\item{register_parallel}{Whether to use parallel processing}

\item{clx}{Computing cluster (legacy parameter, auto-managed now)}

\item{stamp}{Timestamp for marking output files}

\item{prefer_no_features}{Maximum number of features that can be selected}

\item{conda_path}{Path to conda binary (legacy parameter)}

\item{debug}{Enable debug mode with additional output}

\item{timeout_sec}{Timeout in seconds for each method}

\item{type}{Mode parameter (legacy parameter, auto-detected now)}

\item{class_column}{Name of the class column (default: "Class")}

\item{case_label}{Label for positive cases (default: "Case")}

\item{control_label}{Label for negative controls (default: "Control")}

\item{feature_prefix}{Prefix for feature columns (default: "hsa_")}

\item{p_threshold}{P-value threshold for significance (default: 0.05)}

\item{fc_threshold}{Fold change threshold (default: 1.0)}

\item{use_smote}{Whether to apply SMOTE for class balancing (default: TRUE)}

\item{save_intermediate}{Save intermediate results (default: TRUE)}

\item{verbose}{Enable verbose logging (default: TRUE)}
}
\value{
Modern OmicSelector results object (S3 class) with selected features,
  performance metrics, differential expression results, and execution metadata.
  Backward compatible with legacy code expecting formula lists.
}
\description{
Completely rewritten and optimized version of the original OmicSelector_OmicSelector
function. This version is specifically optimized for miRNA Case/Control analysis
with improved performance, error handling, and modern R practices.

Key improvements:
- Optimized for "hsa_" prefixed miRNA features
- Case/Control binary classification focus  
- Modern error handling and validation
- Parallel processing optimization
- Memory-efficient operations
- Comprehensive logging and progress tracking
- Timeout protection for long-running methods
- Intermediate result saving
- Modern S3 class system
}
\examples{
\dontrun{
# Basic usage (backward compatible)
results <- OmicSelector_OmicSelector(
  wd = "path/to/data",
  m = c(1, 2, 4, 11, 17)
)

# Access selected features (new way)
selected_features <- results$selected_features

# Access formulas (legacy compatible)
formulas <- results$formulas

# With custom parameters for miRNA analysis
results <- OmicSelector_OmicSelector(
  wd = "path/to/mirna_data",
  m = c(1, 2, 4, 11, 13, 17),
  max_iterations = 20,
  prefer_no_features = 15,
  p_threshold = 0.01,
  fc_threshold = 1.5,
  case_label = "Case",
  control_label = "Control",
  feature_prefix = "hsa_"
)
}

}
