% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deeplearning.R
\name{predict_transfer_learning}
\alias{predict_transfer_learning}
\alias{OmicSelector_deep_learning_predict_transfered}
\title{Predict Using Transfer Learning Model}
\usage{
predict_transfer_learning(
  model_path = "our_models/transfer_model.zip",
  new_dataset = NULL,
  new_scaling = TRUE,
  old_train_csv_to_restore_scaling = NULL,
  override_cutoff = NULL,
  blinded = TRUE
)

OmicSelector_deep_learning_predict_transfered(
  model_path = "our_models/transfer_model.zip",
  new_dataset = NULL,
  new_scaling = TRUE,
  old_train_csv_to_restore_scaling = NULL,
  override_cutoff = NULL,
  blinded = TRUE
)
}
\arguments{
\item{model_path}{Path to ZIP file containing transfer learning model}

\item{new_dataset}{New dataset for prediction}

\item{new_scaling}{Logical, whether to perform new scaling on data}

\item{old_train_csv_to_restore_scaling}{Path to original training data for scaling}

\item{override_cutoff}{Numeric, custom classification cutoff}

\item{blinded}{Logical, whether validation is blinded (no true labels)}
}
\value{
List containing predictions, performance metrics, and model information
}
\description{
Makes predictions using a transfer learning model (fine-tuned from pre-trained model).
Specifically designed for models that have been adapted via transfer learning.
}
\details{
This function is specifically designed for models created through transfer learning.
It looks for the transfer learning model file (trans_model.hdf5) and uses
transfer learning-specific scaling parameters if available.

The function handles:
\itemize{
  \item Loading transfer learning model from ZIP archive
  \item Applying appropriate scaling (transfer learning specific or original)
  \item Making predictions with optimal cutoff from transfer learning
  \item Comprehensive evaluation if labels are provided
  \item Support for blinded predictions
}
}
\examples{
\dontrun{
# Transfer learning prediction with evaluation
results <- predict_transfer_learning(
  model_path = "models/transfer_model.zip",
  new_dataset = new_data,
  blinded = FALSE
)
print(results$confusion_matrix)

# Blinded prediction
predictions <- predict_transfer_learning(
  model_path = "models/transfer_model.zip",
  new_dataset = new_data,
  blinded = TRUE
)
}

}
\seealso{
\code{\link{transfer_learning_neural_network}}, \code{\link{predict_deep_learning}}
}
