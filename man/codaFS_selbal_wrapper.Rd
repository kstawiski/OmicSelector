% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coda-feature-selection.R
\name{codaFS_selbal_wrapper}
\alias{codaFS_selbal_wrapper}
\title{Selbal-Style Forward Balance Feature Selection}
\usage{
codaFS_selbal_wrapper(
  X,
  y,
  n_features = NULL,
  positive = NULL,
  input_scale = c("log", "raw"),
  backend = c("auto", "official", "fallback"),
  max_terms = NULL,
  min_improvement = 1e-04,
  max_pair_features = 80L,
  prescreen_metric = c("clr_effect", "variance")
)
}
\arguments{
\item{X}{Numeric matrix with samples in rows and features in columns.}

\item{y}{Binary outcome vector.}

\item{n_features}{Optional target panel size. Used as the default
`max_terms`.}

\item{positive}{Optional positive-class label for non-numeric outcomes.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{backend}{One of `"auto"`, `"official"`, or `"fallback"`.}

\item{max_terms}{Maximum number of features in the selected balance. If
`NULL`, defaults to `n_features`.}

\item{min_improvement}{Minimum deviance improvement required to add a new
feature during the fallback forward search.}

\item{max_pair_features}{Maximum number of prescreened features considered by
the forward search.}

\item{prescreen_metric}{Either `"clr_effect"` or `"variance"`.}
}
\value{
A list with per-feature `scores`, `selected_features`, balance
  membership, and backend details.
}
\description{
Wraps the official `selbal` implementation when it is available. In
environments where `selbal` is not installed, or when `backend = "fallback"`
is requested, the function uses a native forward balance-selection routine
that greedily improves univariate logistic deviance on training data only.
}
