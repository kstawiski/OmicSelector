% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modern-heatmap.R
\name{create_modern_heatmap}
\alias{create_modern_heatmap}
\alias{OmicSelector_heatmap}
\title{Modern Heatmap Visualization for Omics Data}
\usage{
create_modern_heatmap(
  expression_data,
  sample_annotations = NULL,
  feature_annotations = NULL,
  scale_data = "zscore",
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_row_names = FALSE,
  show_col_names = TRUE,
  color_palette = NULL,
  title = NULL,
  trim_range = NULL,
  method = "pheatmap",
  ...
)

OmicSelector_heatmap(
  x,
  rlab = NULL,
  zscore = FALSE,
  margins = c(10, 10),
  expression_name = "log10(TPM)",
  trim_min = NULL,
  trim_max = NULL,
  centered_on = NULL,
  legend_pos = "topright",
  legend_cex = 0.8,
  ...
)
}
\arguments{
\item{expression_data}{Matrix or data frame with features in columns and samples in rows}

\item{sample_annotations}{Data frame with sample metadata for annotation tracks}

\item{feature_annotations}{Data frame with feature metadata (optional)}

\item{scale_data}{Character indicating how to scale data: "none", "row", "column", "zscore" (default: "zscore")}

\item{cluster_rows}{Logical indicating whether to cluster rows (default: TRUE)}

\item{cluster_cols}{Logical indicating whether to cluster columns (default: TRUE)}

\item{show_row_names}{Logical indicating whether to show row names (default: FALSE)}

\item{show_col_names}{Logical indicating whether to show column names (default: TRUE)}

\item{color_palette}{Character vector of colors or function to generate colors}

\item{title}{Character string for plot title}

\item{trim_range}{Numeric vector of length 2 for trimming extreme values}

\item{method}{Character indicating plotting method: "ggplot2" or "pheatmap" (default: "pheatmap")}

\item{...}{Additional arguments passed to plotting functions}
}
\value{
ggplot object (if method="ggplot2") or pheatmap object (if method="pheatmap")
}
\description{
Create publication-ready heatmaps for omics data using modern ggplot2 and
pheatmap approaches. Supports multiple annotation tracks and customization options.
}
\examples{
\dontrun{
# Basic heatmap
data <- matrix(rnorm(200), nrow = 20, ncol = 10)
colnames(data) <- paste0("Feature_", 1:10)
rownames(data) <- paste0("Sample_", 1:20)

# Simple heatmap
create_modern_heatmap(data)

# With annotations
annotations <- data.frame(
  Class = factor(rep(c("Case", "Control"), each = 10)),
  Batch = factor(rep(c("A", "B"), 10))
)
create_modern_heatmap(data, sample_annotations = annotations)
}

}
