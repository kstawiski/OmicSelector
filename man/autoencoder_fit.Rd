% Generated by hand
\name{autoencoder_fit}
\alias{autoencoder_fit}
\title{Fit Autoencoder}
\usage{
autoencoder_fit(x, latent_dim = 32L, hidden_layers = c(128L, 64L),
  activation = c("relu", "tanh", "sigmoid", "gelu"), dropout = 0.1,
  epochs = 100L, batch_size = 32L, lr = 0.001, weight_decay = 0,
  loss = c("mse", "mae"), validation_split = 0.1,
  early_stopping = TRUE, patience = 10L, min_delta = 0,
  device = "cpu", seed = NULL, pretrained = NULL,
  freeze_encoder = FALSE)
}
\arguments{
\item{x}{Numeric matrix or data.frame (samples x features).}
\item{latent_dim}{Integer, size of latent representation.}
\item{hidden_layers}{Integer vector of hidden layer sizes.}
\item{activation}{Activation function.}
\item{dropout}{Dropout rate between 0 and 1.}
\item{epochs}{Number of training epochs.}
\item{batch_size}{Batch size.}
\item{lr}{Learning rate.}
\item{weight_decay}{L2 weight decay.}
\item{loss}{Loss function: "mse" or "mae".}
\item{validation_split}{Fraction of data for validation.}
\item{early_stopping}{Logical, enable early stopping.}
\item{patience}{Early stopping patience.}
\item{min_delta}{Minimum improvement to reset patience.}
\item{device}{"cpu" or "cuda".}
\item{seed}{Optional random seed.}
\item{pretrained}{Optional path to saved autoencoder state.}
\item{freeze_encoder}{Logical, freeze encoder weights during training.}
}
\value{
An \code{OmicAutoencoder} object.
}
\description{
Fits a torch autoencoder and returns an OmicAutoencoder object.
}
