% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GoldStandard.R
\name{generate_gold_standard}
\alias{generate_gold_standard}
\title{Generate Gold Standard Synthetic Dataset}
\usage{
generate_gold_standard(
  n_patients = 100,
  samples_per_patient = 3,
  n_features = 1000,
  n_causal = 20,
  n_trap = 30,
  n_batches = 4,
  seed = 42,
  test_ratio = 0.3
)
}
\arguments{
\item{n_patients}{Number of patients (default: 100)}

\item{samples_per_patient}{Number of samples per patient (default: 3)}

\item{n_features}{Total number of features (default: 1000)}

\item{n_causal}{Number of true causal features (default: 20)}

\item{n_trap}{Number of trap/leakage detector features (default: 30)}

\item{n_batches}{Number of batch effects (default: 4)}

\item{seed}{Random seed for reproducibility (default: 42)}

\item{test_ratio}{Ratio of patients for test set (default: 0.3)}
}
\value{
A list of class `OmicSelectorGoldStandard` containing:
\describe{
  \item{data}{Full dataset (data.frame)}
  \item{train_data}{Training split (data.frame)}
  \item{test_data}{Test split (data.frame)}
  \item{train_indices}{Row indices for training}
  \item{test_indices}{Row indices for testing}
  \item{ground_truth}{Named list of feature indices and names}
  \item{train_patient_ids, test_patient_ids}{Patient IDs per split}
  \item{metadata}{Dataset generation parameters}
}
}
\description{
Creates a synthetic dataset with known ground truth for validating
feature selection pipelines against data leakage.
}
\examples{
\dontrun{
# Generate a small dataset for testing
gs <- generate_gold_standard(
  n_patients = 50,
  samples_per_patient = 2,
  n_features = 200,
  seed = 42
)
print(gs)
}

}
