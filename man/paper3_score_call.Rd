% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paper3-method-roster.R
\name{paper3_score_call}
\alias{paper3_score_call}
\title{Score a roster method with the canonical single-sample interface}
\usage{
paper3_score_call(
  method_id,
  model,
  X,
  meta = NULL,
  roster = paper3_method_roster()
)
}
\arguments{
\item{method_id}{Roster method identifier.}

\item{model}{A fitted model object produced by the method's `fit_*` function.}

\item{X}{Numeric matrix (samples x features), or a data frame coercible to one.}

\item{meta}{Optional per-sample metadata (data frame with `nrow(X)` rows).}

\item{roster}{The roster to resolve against; defaults to
[paper3_method_roster()].}
}
\value{
Numeric vector of one score per row of `X`.
}
\description{
Resolves a roster method to its score function and invokes it through the
canonical adapter `function(model, X_rows, meta_rows) -> numeric`. New methods
follow `score_*(model, X, meta = NULL)` directly; legacy ("present") methods
MUST be dispatched through a shim registered with
[paper3_register_score_adapter()] (their argument order predates the canonical
convention, so calling them positionally would mis-bind). Methods whose score
function is not yet implemented raise an explicit not-yet-implemented error.
}
\seealso{
[paper3_method_roster()], [paper3_assert_row_equivariant()]
}
