% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coda-feature-selection.R
\name{codaFS_logcontrast_lasso}
\alias{codaFS_logcontrast_lasso}
\title{Log-Contrast Lasso on CLR Features}
\usage{
codaFS_logcontrast_lasso(
  X,
  y,
  n_features = NULL,
  positive = NULL,
  input_scale = c("log", "raw"),
  alpha = 1,
  nfolds = 5L,
  nlambda = 100L,
  lambda_rule = c("lambda.1se", "lambda.min")
)
}
\arguments{
\item{X}{Numeric matrix with samples in rows and features in columns.}

\item{y}{Binary outcome vector.}

\item{n_features}{Optional target panel size.}

\item{positive}{Optional positive-class label for non-numeric outcomes.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{alpha}{Elastic-net mixing parameter passed to `glmnet`.}

\item{nfolds}{Number of cross-validation folds used by `cv.glmnet`.}

\item{nlambda}{Number of lambda values used by `cv.glmnet`.}

\item{lambda_rule}{Either `"lambda.1se"` (default) or `"lambda.min"`.}
}
\value{
A list with per-feature `scores`, the raw and zero-sum projected
  coefficients, and `selected_features`.
}
\description{
Fits a lasso or elastic-net logistic model on centered log-ratio (CLR)
features, then projects the coefficient vector onto the zero-sum hyperplane
to obtain a valid log-contrast representation. Feature importance is the
absolute projected coefficient magnitude.
}
