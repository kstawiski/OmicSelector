% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modern-volcano-plot.R
\name{create_volcano_plot}
\alias{create_volcano_plot}
\alias{OmicSelector_vulcano_plot}
\title{Modern Volcano Plot for Differential Expression Analysis}
\usage{
create_volcano_plot(
  de_results,
  feature_column = "feature",
  fc_column = "log2FoldChange",
  pval_column = "pvalue",
  padj_column = "padj",
  selected_features = NULL,
  label_features = NULL,
  use_adjusted_p = TRUE,
  fc_threshold = 1,
  p_threshold = 0.05,
  point_size = 1.5,
  label_size = 3,
  colors = c(significant = "#E31A1C", selected = "#1F78B4", ns = "grey60"),
  title = "Volcano Plot",
  ...
)

OmicSelector_vulcano_plot(
  selected_miRNAs,
  DE = NULL,
  only_label = NULL,
  take_adjusted_p = FALSE
)
}
\arguments{
\item{de_results}{Data frame with differential expression results containing columns:
log2FoldChange, pvalue (and optionally padj for adjusted p-values)}

\item{feature_column}{Character string indicating column name with feature identifiers (default: "feature")}

\item{fc_column}{Character string indicating fold change column name (default: "log2FoldChange")}

\item{pval_column}{Character string indicating p-value column name (default: "pvalue")}

\item{padj_column}{Character string indicating adjusted p-value column name (default: "padj")}

\item{selected_features}{Character vector of features to highlight}

\item{label_features}{Character vector of features to label (subset of selected_features)}

\item{use_adjusted_p}{Logical indicating whether to use adjusted p-values (default: TRUE)}

\item{fc_threshold}{Numeric fold change threshold for significance (default: 1)}

\item{p_threshold}{Numeric p-value threshold for significance (default: 0.05)}

\item{point_size}{Numeric size for points (default: 1.5)}

\item{label_size}{Numeric size for labels (default: 3)}

\item{colors}{Named character vector with colors for different point types}

\item{title}{Character string for plot title}

\item{...}{Additional arguments passed to ggplot}
}
\value{
ggplot object
}
\description{
Create publication-ready volcano plots for differential expression analysis
using modern ggplot2 with customizable highlighting and labeling.
}
\examples{
\dontrun{
# Create sample differential expression data
de_data <- data.frame(
  feature = paste0("miR_", 1:100),
  log2FoldChange = rnorm(100, 0, 2),
  pvalue = runif(100, 0, 0.1),
  padj = p.adjust(runif(100, 0, 0.1), method = "BH")
)

# Basic volcano plot
create_volcano_plot(de_data)

# Highlight specific features
selected <- de_data$feature[1:10]
create_volcano_plot(de_data, selected_features = selected)
}

}
