% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coda-feature-selection.R
\name{codaFS_stability_logratio}
\alias{codaFS_stability_logratio}
\title{Stability Selection on Pairwise Log-Ratios}
\usage{
codaFS_stability_logratio(
  X,
  y,
  n_features = NULL,
  positive = NULL,
  input_scale = c("log", "raw"),
  alpha = 0.9,
  subsample_frac = 0.7,
  n_subsamples = 20L,
  nfolds = 3L,
  nlambda = 60L,
  lambda_rule = c("lambda.1se", "lambda.min"),
  selection_threshold = 0.6,
  max_pair_features = 60L,
  prescreen_metric = c("clr_effect", "variance")
)
}
\arguments{
\item{X}{Numeric matrix with samples in rows and features in columns.}

\item{y}{Binary outcome vector.}

\item{n_features}{Optional target panel size.}

\item{positive}{Optional positive-class label for non-numeric outcomes.}

\item{input_scale}{Either `"log"` (default) or `"raw"`.}

\item{alpha}{Elastic-net mixing parameter.}

\item{subsample_frac}{Fraction of samples used in each stability iteration.}

\item{n_subsamples}{Number of stability iterations.}

\item{nfolds}{Number of cross-validation folds within each subsample.}

\item{nlambda}{Number of lambda values used by `cv.glmnet`.}

\item{lambda_rule}{Either `"lambda.1se"` (default) or `"lambda.min"`.}

\item{selection_threshold}{Frequency threshold used to define stable ratios.}

\item{max_pair_features}{Maximum number of prescreened features retained
before constructing pairwise ratios.}

\item{prescreen_metric}{Either `"clr_effect"` or `"variance"`.}
}
\value{
A list with per-feature `scores`, stable pairwise log-ratio
  frequencies, and `selected_features`.
}
\description{
Applies subsampling-based stability selection to pairwise log-ratio features
using elastic-net logistic models. Stable ratios are aggregated back to
individual features by their incidence-weighted selection frequency.
}
