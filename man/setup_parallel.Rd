% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{setup_parallel}
\alias{setup_parallel}
\title{Configure Parallelization for OmicSelector}
\usage{
setup_parallel(
  workers = NULL,
  plan = c("multisession", "multicore", "sequential"),
  memory_limit = 500 * 1024^2
)
}
\arguments{
\item{workers}{Number of workers (cores) to use. Default is available cores - 1.}

\item{plan}{Parallelization plan: "multisession" (default, works everywhere),
"multicore" (Unix only, more memory efficient), "sequential" (no parallelism)}

\item{memory_limit}{Per-worker memory limit in bytes (future.globals.maxSize)}
}
\value{
Invisible previous plan (for restoration)
}
\description{
Sets up parallel processing using the future package.
Call this before running benchmarks or stability computations.
}
\examples{
\dontrun{
# Use 4 cores
setup_parallel(workers = 4)

# Run your analysis
result <- service$run(parallel = TRUE)

# Reset to sequential
reset_parallel()
}

}
