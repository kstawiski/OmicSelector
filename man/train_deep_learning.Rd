% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deeplearning.R
\name{train_deep_learning}
\alias{train_deep_learning}
\alias{OmicSelector_deep_learning}
\title{Deep Learning Training for miRNA Biomarker Selection}
\usage{
train_deep_learning(
  selected_miRNAs = ".",
  wd = getwd(),
  SMOTE = FALSE,
  keras_batch_size = 64,
  clean_temp_files = TRUE,
  save_threshold_trainacc = 0.85,
  save_threshold_testacc = 0.8,
  keras_epochae = 1000,
  keras_epoch = 500,
  keras_patience = 50,
  hyperparameters = NULL,
  add_features_to_predictions = FALSE,
  keras_threads = ceiling(parallel::detectCores()/2),
  start = 1,
  end = NULL,
  output_file = "deeplearning_results.csv",
  save_all_vars = FALSE,
  automatic_weight = FALSE
)

OmicSelector_deep_learning(
  selected_miRNAs = ".",
  wd = getwd(),
  SMOTE = FALSE,
  keras_batch_size = 64,
  clean_temp_files = TRUE,
  save_threshold_trainacc = 0.85,
  save_threshold_testacc = 0.8,
  keras_epochae = 1000,
  keras_epoch = 500,
  keras_patience = 50,
  hyperparameters = NULL,
  add_features_to_predictions = FALSE,
  keras_threads = ceiling(parallel::detectCores()/2),
  start = 1,
  end = NULL,
  output_file = "deeplearning_results.csv",
  save_all_vars = FALSE,
  automatic_weight = FALSE
)
}
\arguments{
\item{selected_miRNAs}{Character vector of miRNA names, or '.' for all hsa miRNAs}

\item{wd}{Working directory containing mixed_train.csv, mixed_test.csv, mixed_validation.csv}

\item{SMOTE}{Logical, whether to use SMOTE for class balancing}

\item{keras_batch_size}{Integer, batch size for neural network training}

\item{clean_temp_files}{Logical, whether to clean temporary files after completion}

\item{save_threshold_trainacc}{Minimum training accuracy threshold to save model}

\item{save_threshold_testacc}{Minimum test accuracy threshold to save model}

\item{keras_epochae}{Maximum epochs for autoencoder training}

\item{keras_epoch}{Maximum epochs for classification model training}

\item{keras_patience}{Early stopping patience (epochs without improvement)}

\item{hyperparameters}{Dataframe with hyperparameter grid for model search}

\item{add_features_to_predictions}{Whether to include all features in prediction output}

\item{keras_threads}{Number of parallel threads for model training}

\item{start}{Starting index in hyperparameter grid}

\item{end}{Ending index in hyperparameter grid}

\item{output_file}{Output CSV filename for results}

\item{save_all_vars}{Whether to save all variables in model workspace}

\item{automatic_weight}{Whether to use automatic class weighting}
}
\value{
List containing:
\describe{
  \item{results}{Dataframe with performance metrics for each model}
  \item{best_models}{Information about best performing models}
  \item{model_paths}{Paths to saved model files}
  \item{status}{Completion status and summary}
}
}
\description{
Comprehensive deep learning training pipeline with hyperparameter optimization,
autoencoder support, and extensive model evaluation for miRNA biomarker analysis.
}
\details{
This function implements a comprehensive deep learning pipeline that includes:
\itemize{
  \item Hyperparameter grid search across network architectures
  \item Optional autoencoder pre-training for feature reduction
  \item Parallel model training with early stopping
  \item Comprehensive model evaluation on train/test/validation sets
  \item Automatic model saving based on performance thresholds
  \item ROC analysis and optimal cutpoint determination
  \item Support for class balancing via SMOTE or automatic weighting
}

The hyperparameters dataframe should contain columns for network architecture:
layer1, layer2, layer3 (neuron counts), activation functions, dropout rates,
regularization flags, optimizer choice, autoencoder settings, and scaling options.
}
\examples{
\dontrun{
# Basic deep learning with default hyperparameters
results <- train_deep_learning(
  selected_miRNAs = '.',  # Use all hsa_ miRNAs
  wd = 'data/',
  keras_epoch = 500,
  keras_threads = 2
)

# Advanced hyperparameter search
custom_grid <- expand.grid(
  layer1 = c(32, 64, 128),
  layer2 = c(0, 16, 32),
  layer3 = 0,
  activation_function_layer1 = c('relu', 'sigmoid'),
  activation_function_layer2 = 'relu',
  activation_function_layer3 = 'relu',
  dropout_layer1 = c(0, 0.1, 0.2),
  dropout_layer2 = 0,
  dropout_layer3 = 0,
  layer1_regularizer = c(TRUE, FALSE),
  layer2_regularizer = FALSE,
  layer3_regularizer = FALSE,
  optimizer = c('adam', 'rmsprop'),
  autoencoder = c(0, 7),  # 0 = no autoencoder, 7 = 7-neuron bottleneck
  balanced = FALSE,
  scaled = TRUE,
  stringsAsFactors = FALSE
)

results <- train_deep_learning(
  hyperparameters = custom_grid,
  save_threshold_trainacc = 0.90,
  save_threshold_testacc = 0.85
)
}

}
\seealso{
\code{\link{create_keras_model}}, \code{\link{predict_deep_learning}}
}
