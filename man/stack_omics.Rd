% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi-omics.R
\name{stack_omics}
\alias{stack_omics}
\title{Create Multi-Omics Stacked Ensemble (Convenience Function)}
\usage{
stack_omics(
  data_list,
  learner_list = NULL,
  y = NULL,
  meta_learner = NULL,
  resampling = NULL,
  task_type = "classif"
)
}
\arguments{
\item{data_list}{Named list of data matrices (one per modality)}

\item{learner_list}{Optional named list of learners (or single learner for all)}

\item{y}{Optional target vector (if provided, trains immediately)}

\item{meta_learner}{Learner for stacking}

\item{resampling}{Resampling strategy}

\item{task_type}{"classif" or "regr"}
}
\value{
OmicStackedEnsemble object
}
\description{
High-level interface for creating and optionally training a stacked ensemble.
}
\examples{
\dontrun{
# Create and train ensemble
ensemble <- stack_omics(
  data_list = list(mRNA = rna_matrix, miRNA = mirna_matrix),
  learner_list = list(mRNA = "classif.ranger", miRNA = "classif.glmnet"),
  y = y_vector,
  meta_learner = "classif.log_reg"
)

# Predict
preds <- ensemble$predict(list(mRNA = new_rna, miRNA = new_mirna))
}

}
