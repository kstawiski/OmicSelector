% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/singlesample-deploy.R
\name{is_singlesample_deployable}
\alias{is_singlesample_deployable}
\title{Check whether a deployment scorer is single-sample deployable}
\usage{
is_singlesample_deployable(deployable, X_probe = NULL)
}
\arguments{
\item{deployable}{A \code{singlesample_deployable} object from
\code{\link{deploy_singlesample}}.}

\item{X_probe}{Numeric matrix/data frame of probe rows. If \code{NULL}, the
check cannot be run; callers should supply probe rows from the intended
panel or validation surface.}
}
\value{
\code{TRUE} when the probe check passes, otherwise \code{FALSE}.
}
\description{
User-facing wrapper over \code{\link{singlesample_is_row_equivariant}}. Supply
representative probe rows in \code{X_probe}; the check asks whether each
specimen's singleton score equals the same row's score inside a batch, with
the model state unchanged during scoring.
}
