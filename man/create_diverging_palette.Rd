% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/OmicSelector_diverge_color.R
\name{create_diverging_palette}
\alias{create_diverging_palette}
\alias{OmicSelector_diverge_color}
\title{Create Diverging Color Palette for Heatmaps}
\usage{
create_diverging_palette(
  data,
  centered_on = 0,
  n_colors = 100,
  colors = c("blue", "white", "red"),
  symmetric = TRUE
)

OmicSelector_diverge_color(data, centeredOn = 0)
}
\arguments{
\item{data}{Numeric vector or matrix of values}

\item{centered_on}{Numeric value to center the color scale on (default: 0)}

\item{n_colors}{Integer number of colors to generate (default: 100)}

\item{colors}{Character vector of colors for the palette (default: blue-white-red)}

\item{symmetric}{Logical indicating whether to make the scale symmetric around center (default: TRUE)}
}
\value{
List containing:
  \item{breaks}{Numeric vector of break points for the color scale}
  \item{colors}{Character vector of colors corresponding to breaks}
  \item{cuts}{Cut intervals for mapping data to colors}
}
\description{
Modern helper function to create diverging color palettes centered on a specific value.
Useful for heatmap visualization where you want to emphasize deviations from a baseline.
}
\examples{
\dontrun{
# Basic usage
data <- rnorm(100)
palette <- create_diverging_palette(data)

# Custom center and colors
palette <- create_diverging_palette(data, centered_on = 1, 
                                  colors = c("green", "white", "purple"))
}

}
