# Extract Consensus Features from Selected Features Per Fold

Extract Consensus Features from Selected Features Per Fold

## Usage

``` r
.extract_consensus_from_sf(sf, learner_id, min_frequency)
```
