# OmicSelector_table

A wrapper to draw pretty tables in rmarkdown document

## Usage

``` r
OmicSelector_table(table, height = "400px", ...)
```

## Arguments

- table:

  Table to draw.

- hight:

  High (default: 400px)
