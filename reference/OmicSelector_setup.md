# OmicSelector_setup

Run this function to be sure that everything is installed properly for
OmicSelector.

## Usage

``` r
OmicSelector_setup(keras = TRUE, msg = TRUE)
```

## Arguments

- keras:

  Try to install keras using install_keras()?

- msg:

  Display OK message at the end?
