# Print a single-sample deployment object

Print a single-sample deployment object

## Usage

``` r
# S3 method for class 'singlesample_deployable'
print(x, ...)
```

## Arguments

- x:

  A `singlesample_deployable` object.

- ...:

  Unused.

## Value

Invisibly, `x`.
