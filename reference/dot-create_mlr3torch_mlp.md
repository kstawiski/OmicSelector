# Create mlr3torch MLP (internal)

Create mlr3torch MLP (internal)

## Usage

``` r
.create_mlr3torch_mlp(
  hidden_layers,
  dropout,
  activation,
  epochs,
  batch_size,
  lr,
  early_stopping,
  patience,
  device
)
```
