# Register CoDA Feature Selection Filters

Registers the CoDA-aware \`mlr3filters\` feature filters provided by
this package. The function is idempotent and is also called on package
load.

## Usage

``` r
register_coda_feature_selection_filters()
```
